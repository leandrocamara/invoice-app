
/**
 * Mensagens de Validação (VeeValidate)
 */
const dictionary = {
  pt: {
    messages: {
      _default: (field) => `Informe um valor válido`,
      required: (field) => `Campo obrigatório`
    }
  }
}

export default dictionary
